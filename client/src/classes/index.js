/**So that every classes inside this folder can be used outside without the need of 
 * writing it into the index.html
 */

// import * as compound from './compound';
import { PeriodicTable } from './periodic-table.js';